---
plugin: redhat.openshift_virtualization.kubevirt
connections:
  - host: https://api.g4gmx.dynamic.redhatworkshops.io:6443
    # api_key: '{{ ocp_api_key }}'
    username: '{{ ocp_username }}'
    password: '{{ ocp_password }}'
    validate_certs: false
    use_service: False
    use_extra_vars: true