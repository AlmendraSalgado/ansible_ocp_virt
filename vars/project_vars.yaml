---
namespace: "{{ project_dir }}"
namespace_description: | 
  Quick demo
namespace_display_name: "{{ project_dir }}"

application_name: webapp

# template_name: fevermap-mysql-persistent
# template_parameters:
#   NAMESPACE: "{{ namespace }}"
#   NAME: "{{ application_name }}"